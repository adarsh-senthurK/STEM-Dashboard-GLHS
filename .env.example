# Copy to .env.local — values from Supabase dashboard → Settings → API Keys.
# The publishable (anon) key is safe for browsers; RLS protects the data.
VITE_SUPABASE_URL=https://YOUR-PROJECT.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_...
